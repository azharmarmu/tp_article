About:


If it is Widget add constructor, else ignore
Constructor: (snippet)
const X(
    {
        Key key,
    }
)

If it is Widget add Properties, else ignore
Properties of X Widget:

Example: In this example, we will see how we can use 'X'.

First, we will see an app without the 'X' being used.

Code:

Output:(screen-shot/gif)

Explanation:


Now, we will see an app with the 'X' being used.

Code:

Output:(screen-shot/gif)

Explanation:


